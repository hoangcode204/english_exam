server:
  port: 8080
  servlet:
    context-path: /api

spring:
  ai:
    openai:
      api-key: AIzaSyBMT6EwdTobiK7DRf2RHNxpHoEVbIZl_VA
      chat:
        base-url: https://generativelanguage.googleapis.com
        completions-path: /v1beta/openai/chat/completions
        options:
          model: gemini-2.0-flash
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB   # Giới hạn dung lượng tối đa của mỗi file (ở đây là 10MB)
      max-request-size: 20MB  # Giới hạn dung lượng tối đa của toàn bộ request
  datasource:
    url: "jdbc:mysql://localhost:3306/elearning"
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sendGrid:
    apiHung: ${SENDGRID_KEY}
#    apiKey: ${SENDGRID_API_KEY}
    fromEmail: mkvht8@gmail.com
    templateId: d-d82439f102b74bc2b935fed16fa0548f
    resetPasswordId: d-b4e100ac2dbd4024a249240aff3035d5
    verificationLink: http://localhost:8080/api/users/confirm-email

jwt:
  signerKey: ${JWT_KEY}
  valid-duration: 1 # in second
  refreshable-duration: 1 # in seconds

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
openapi:
  service:
    api-docs: backend-service
    server: http://localhost:${server.port}/api
    title: Backend service
    version: 1.0.0

aws:
  s3.secret-key: ${S3_SECRET_KEY}
  s3.access-key: ${S3_ACCESS_KEY}
  s3.bucket-name: "hung-hotel-images"

# Whisper Service Configuration
whisper:
  service:
    url: http://localhost:8000  # URL of Whisper FastAPI service
    timeout: 30000              # Timeout in milliseconds (30 seconds)
